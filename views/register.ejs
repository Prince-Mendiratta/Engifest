<!DOCTYPE html>
<html>

<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">

  <link rel="shortcut icon" href="http://localhost:3000/favicon.ico">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="theme-color" content="#000000">
  <!-- Compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <title>Engifest</title>
  <style type="text/css">
    body {
      margin: 0;
      padding: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", "Oxygen",
        "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue",
        sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    code {
      font-family: source-code-pro, Menlo, Monaco, Consolas, "Courier New",
        monospace;
    }
  </style>
  <style type="text/css"></style>
</head>

<body>

  <div class="navbar-fixed">
    <nav class="z-depth-0">
      <div class="nav-wrapper white"><a class="col s5 brand-logo center black-text" href="/"
          style="font-family: monospace;"><img style="max-width:32px; margin-right: 15px;"
            src="/images/Engifest_Logo.jpg">ENGIFEST</a></div>
    </nav>
  </div>

  <div class="container">
    <div class="row">
      <div class="col s8 offset-s2"><a class="btn-flat waves-effect" href="/"><i
            class="material-icons left">keyboard_backspace</i> Back to home</a>
        <div class="col s12" style="padding-left: 11.25px;">
          <h4><b>Register</b> below</h4>
          <p class="grey-text text-darken-1">Already have an account? <a href="/login">Log
              in</a></p>
        </div>

        <form novalidate="">
          <div class="input-field col s12"><input id="name" type="text" class="
            <% if(typeof errors !='undefined' ) { %>
              <%= errors.name.class %>
            <% } %>
            " value=""><label for="name">Name</label><span class="red-text">
              <% if(typeof errors !='undefined' ) { %>
                <%= errors.name.error %>
                  <% } %>
            </span></div>
          <div class="input-field col s12"><input id="email" type="email" class="
            <% if(typeof errors !='undefined' ) { %>
              <%= errors.email.class %>
            <% } %>
            " value=""><label for="email">Email</label><span class="red-text"></span>
            <% if(typeof errors !='undefined' ) { %>
              <%= errors.email.error %>
                <% } %>
          </div>
          <div class="input-field col s12"><input id="password" type="password" class="
            <% if(typeof errors !='undefined' ) { %>
              <%= errors.password.class %>
            <% } %>
            " value=""><label for="password">Password</label><span class="red-text">
              <% if(typeof errors !='undefined' ) { %>
                <%= errors.password.error %>
                  <% } %>
            </span></div>
          <div class="input-field col s12"><input id="password2" type="password" class="
            <% if(typeof errors !='undefined' ) { %>
              <%= errors.password2.class %>
            <% } %>
            " value=""><label for="password2">Confirm Password</label><span class="red-text">
              <% if(typeof errors !='undefined' ) { %>
                <%= errors.password2.error %>
                  <% } %>
            </span></div>
          <div class="col s12" style="padding-left: 11.25px;"><button type="submit"
              class="btn btn-large waves-effect waves-light hoverable blue accent-3"
              style="width: 150px; border-radius: 3px; letter-spacing: 1.5px; margin-top: 1rem;">Sign up</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

</body>

</html>